1. Install DDEV environment following blow link https://ddev.com/get-started/ (use other lamp environment if you have).
2. Run ```ddev start``` under project directory.
3. Run ```ddev drush si --existing-config``` command after ddev start(use ```drush si --existing-config``` in your server if you use other lamp environment).
4. Run ```ddev drush dcdi```
